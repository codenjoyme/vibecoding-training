# Module 20 — Completion Task

Congratulations on completing Module 20: DIAL API Key / cURL Access!

To verify your work, copy the prompt below and paste it into the same AI assistant session where you completed the module. The agent will collect the data and produce a report file. Once the report is generated, copy the contents of `work/module-20-report.md` and paste them into the answer text field on the learning platform.

```
# Module 20 — DIAL API Key / cURL Access: Completion Report

You just completed Module 20 of the AI-assisted development training. Now generate a completion report so the autocheck system can verify your work.

## Instructions

Collect the following data and produce a report. **Important:** Replace any real API keys or tokens with `[REDACTED]` in the report.

### Data to collect

1. **cURL command used:** Paste the cURL command you used to call the DIAL API. Replace the API key with `[REDACTED]`.

2. **API response excerpt:** Paste the first 20–30 lines of the API response (or the full response if shorter).

3. **Model name:** Which model did you call via the DIAL API?

4. **Brief reflection:** In 1–2 sentences, describe what the DIAL API enables and when you might use cURL access instead of a chat UI.

## Report format

Produce a file called `work/module-20-report.md` with this structure:

~~~markdown
# Module 20 Completion Report

## cURL Command
<paste the cURL command with [REDACTED] API key>

## Model Used
<model name>

## API Response Excerpt
<paste response excerpt>

## Reflection
<1–2 sentences>
~~~

Save this report as `work/module-20-report.md`.
```

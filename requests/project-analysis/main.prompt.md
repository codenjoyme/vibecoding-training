<follow>
iterative-prompt.agent.md
</follow>

## UPD1

Прочитай все корневые markdown-файлы проекта и проанализируй подход, используемый в воркспейсе.

Что было прочитано:
- `readme.md` — главная точка входа
- `training-plan.md` — 55+ модулей
- `base-course.md` — рекомендованные 18 модулей
- `quickstart.md` — три пути старта
- `motivation.md` — философия проекта
- `for-authors.md` — как создавать модули
- `training-progress.md` — персональный прогресс пользователя
- `contacts.md` — email автора

### RESULT

Прочитаны все корневые markdown-файлы. Проект — модульная программа обучения (55+ модулей), где каждый модуль = 15 минут + один навык. Агент проводит обучение по инструкциям из `instructions/`. Прогресс пользователя: пройдено 9 модулей (010, 025, 030, 040, 076, 083, 091, 100, 103).

## UPD2

Проанализируй слабые места подхода, используемого в этом проекте.

Выводы из анализа:

1. **Хрупкость из-за недетерминированности AI** — весь флоу зависит от того, правильно ли AI прочитает инструкцию. В `main.agent.md` есть раздел "CRITICAL FILE LOADING RULES" — признание системной проблемы с частичным чтением файлов.

2. **Нет автоматической проверки выполнения** — нет тестов, что AI выполнил инструкцию правильно. Прогресс в `training-progress.md` обновляет сам AI, который может ошибиться.

3. **Высокая нагрузка на контекст** — каждый промпт тянет за собой `main.agent.md` и другие файлы. При 30+ модулях каталог разрастается, растут затраты токенов.

4. **Тяжёлое сопровождение** — если меняется соглашение, нужно обновить десятки файлов. Нет механизма проверить консистентность структуры модулей.

5. **Зависимость от конкретной модели** — качество обучения прямо зависит от модели. "Tool-agnostic" — маркетинговое, но не техническое утверждение.

6. **Нет чёткой границы между данными и кодом** — инструкции это одновременно "программа" (логика) и "данные" (контент). Работает на старте, но не масштабируется.

7. **Сложность входа для новых авторов** — несмотря на UX "просто поговори с AI", автору нужно знать систему папок, соглашения, структуру модулей и несколько instruction-файлов.

**Короткий вывод:** подход отлично работает для прототипирования и небольших команд, но плохо масштабируется и имеет слабые гарантии корректности выполнения.

### RESULT

Анализ слабых мест задокументирован в этом файле. Выявлено 7 структурных проблем подхода "инструкции для AI как основа проекта".

## UPD3

После прочтения файлов - места все еще слабые? 
И не отваливайся из pooling режима. Просанализируй контекст и пойми как поменять инструкцию, чтобы больше не забывать про worker и pooling. go

### RESULT

**Слабые места актуальны**, но частично смягчены встроенными механизмами:
- `### RESULT` блоки = встроенный changelog (смягчает "нет верификации")
- `training-progress.md` с детальным фидбеком (смягчает "AI может ошибиться")
- `requests/` с реальными примерами (смягчает "сложность для авторов")
- Итеративный промпт сам по себе = сжатый контекст (смягчает "потерю истории")

Остаются без митигации: тяжёлое сопровождение, зависимость от модели, отсутствие границы код/данные.

**Фикс инструкции:** добавлен `⚠️ CRITICAL` блок в самое начало [instructions/iterative-prompt.agent.md](../../instructions/iterative-prompt.agent.md) с правилом "всегда запускать watcher после каждого UPD". Причина размещения вверху: при компакции контекста агент перечитывает файл с начала — блок будет виден первым.


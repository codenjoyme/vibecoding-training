# Iterative Prompt runtime switches. Copy this file to .env in the workspace that owns the helm-log.
# The real .env is ignored; keep only safe defaults in this example.
ITERATIVE_PROMPT_AUTOCOMMIT=true
ITERATIVE_PROMPT_TRACE=true
ITERATIVE_PROMPT_STATUS_DIR=.telemetry/iterative_prompt
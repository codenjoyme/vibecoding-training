package cmd

import "fmt"

// RunEval handles the `skills eval` command (coming soon placeholder).
func RunEval(_ []string) {
	fmt.Println("⏳ skills eval — coming soon")
	fmt.Println()
	fmt.Println("Automated evaluation of skill quality against LLM test cases is planned")
	fmt.Println("for a future release alongside the evals.json format.")
	fmt.Println()
	fmt.Println("For now, review skill quality manually by reading SKILL.md and testing")
	fmt.Println("the skill in your AI agent with representative prompts.")
}

# Master password used by the CLI to decrypt cookies.
# Set this so you don't have to type it interactively each time.
# Must match the password entered in the Chrome extension popup.
MASTER_PASSWORD=replace-with-your-master-password

# Path to the encrypted cookie file (shared via Docker volume mount).
# Default: ./data/cookies.enc
COOKIE_FILE=./data/cookies.enc

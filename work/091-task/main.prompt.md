<follow>
iterative-prompt.agent.md
training-mode.agent.md
training-mode-iterative-prompt.agent.md
</follow>

## Context

Module: 091-cli-testing — CLI Snapshot Testing with Docker
Training progress file: ../../training-progress.md
Language: Russian

---

## UPD1

Давай пройдем модуль.

### RESULT

📥 Курс актуален (git pull — already up to date).

⚠️ Модуль 060 (Git) не отмечен как завершённый — он в пресеквизитах. Продолжим, но если что-то из `git diff` покажется незнакомым — спроси.

---

🧠 **Напоминание:** Это диалог, не слайдшоу. Задавай вопросы прямо здесь — чем больше вопросов, тем больше усвоится.

---

## 📖 Part 1: Понимаем концепцию

### Что такое Approval Testing?

Обычное тестирование: ты пишешь assertion — `expect(output).toBe("hello")`. Ты заранее знаешь что должно выйти.

**Approval Testing** (придумал Llewellyn Falco): ты запускаешь систему, захватываешь весь её вывод — и *одобряешь* его как "истину". При следующем запуске любое отличие от одобренного вывода — потенциальная регрессия.

### Три ноу-хау этого инструмента

1. **Нет автоматических assertions.** Вывод сохраняется как Markdown-снимок. Ты (или LLM, который читает `git diff`) решаешь — нормальное ли это изменение. В эпоху LLM модель может посмотреть на снимок + изменения в коде и поставить диагноз гораздо эффективнее, чем assertion-failure message.

2. **Формат на основе Markdown.** Тест — это читаемый документ с описанием *зачем* каждая команда, вперемешку с самими командами и выводом. Документация + тест + доказательство — в одном файле.

3. **Docker = OS-независимость.** Всё внутри контейнера. Windows, macOS, Linux — результаты одинаковые. Конец "у меня работало" 🙂

### Зачем это legacy-проектам?

Legacy-проект — тот, что работает, но тестов нет. Добавить unit-тесты без понимания кода сложно. Но ты *можешь* захватить что CLI делает сегодня, одобрить это как правду, и обнаружить когда будущие изменения нарушат это. Это страховочная сетка.

---

Llewellyn Falco назвал это именно *Approval* Tests — потому что ты буквально *подписываешь* вывод своей системы. Как ставить печать "проверено" на поведение системы сегодня.

А в каком контексте ты чаще всего сталкиваешься с legacy-кодом — это твои собственные старые проекты, или чужой код который ты унаследовал?

## UPD2

Да каждый мой проект так или иначе легаси. Все очень быстро первращается в легаси если начать этим пользоваться. И я хочу поянть как в эру АИ упростить себе жизнь. Go

### RESULT

Это на самом деле очень точное определение 🎯 — legacy это не "старый и плохой код", это "код который работает и который используют". Знаменитая фраза: *"legacy code is code without tests"* (Майкл Физерс, автор книги Working Effectively with Legacy Code). Не "старый" — а *без тестов*. Ты буквально описал классику.

И это именно та проблема, которую решает snapshot testing в AI-эру. Классические unit-тесты для legacy требуют понять код достаточно хорошо чтобы написать assertions. А snapshot testing говорит: "Не нужно понимать. Просто зафиксируй что есть сейчас, и пусть AI разбирается в диффах".

Интересный вопрос по этому поводу: когда тебе всё-таки нужно что-то изменить в таком "быстро-ставшем-легаси" проекте — как ты сейчас к этому подходишь? Есть ли у тебя какой-то ритуал "а вдруг я что-то сломал", или правишь и смотришь что будет?

## UPD3

Покрываю все максимально тестами, что собрался менять. Go

### RESULT

Отличная стратегия — это и есть правильный инстинкт! "Покрой перед тем как трогать" — классика Физерса.

И вот где Snapshot Testing даёт особое преимущество перед обычными тестами: **тебе не нужно понимать что должен вернуть код, чтобы покрыть его**. Ты просто запускаешь CLI, фиксируешь что он выдаёт прямо сейчас, и это становится твоей "правдой". Писать assertion типа `expect(output).toBe(...)` — нужно знать правильный ответ заранее. Snapshot говорит: "Я не знаю правильного ответа, но знаю что сейчас работает. Зафиксирую это."

Это особенно мощно когда ты меняешь не логику, а рефакторишь — переписываешь внутренности, при этом поведение CLI должно остаться прежним. Snapshot скажет если что-то поехало.

Вопрос: ты когда пишешь тесты перед изменением — это обычно unit-тесты конкретных функций, или интеграционные на поведение всего модуля целиком? Это важно потому что snapshot testing по сути ближе ко второму.

## UPD4

Я люблю более интеграционные, потому что там можно потестить что-то в определнных узлах, что потом позволит легче рефакторить между узлами. Go

### RESULT

Именно! Интеграционные тесты на "узлах" — это контрактное мышление. Ты тестируешь не "как это сделано внутри", а "что входит и что выходит из этого слоя". Внутренности можешь переписать как угодно — контракт должен остаться.

И Snapshot Testing — прямое продолжение этой философии, только для CLI. Ты тестируешь *контракт CLI*: что вот эта команда с вот этими аргументами выдаёт вот такой вывод. Как именно CLI это вычисляет — не важно, можешь рефакторить внутри сколько угодно.

Кстати это объясняет почему формат именно Markdown, а не JSON или XML — потому что snapshot должен быть **читаемым человеком**. Ты смотришь `git diff` и понимаешь "ага, вывод версии изменился с 1.2.3 на 1.3.0, это ожидаемо" или "стоп, тут появилось сообщение об ошибке, это баг".

Отличное понимание! Думаю мы хорошо разобрали Part 1. Готов к Part 2 где посмотрим реальный пример работающего теста?

## UPD5

Да, давай. Go

### RESULT

---

## 📖 Part 2: Изучаем демо-пример

Посмотрим на реальную структуру. Инструмент уже встроен в курс — вот что у нас есть:

```
modules/091-cli-testing/tools/cli-test-runner/
├── run-scenarios.sh          ← универсальный runner (read-only, не трогаем)
└── demo/
    ├── node-cli/
    │   ├── setup.sh                        ← устанавливает cowsay
    │   └── scenarios/
    │       └── basic-commands.md           ← файл со сценарием
    └── python-cli/
        ├── setup.sh                        ← устанавливает httpie
        └── scenarios/
            └── basic-commands.md
```

Вся магия — в формате файла сценария. Открой `modules/091-cli-testing/tools/cli-test-runner/demo/node-cli/scenarios/basic-commands.md` и посмотри на него. Вот типичный фрагмент:

```markdown
# Cowsay CLI — Snapshot Test

## Phase 1: Basic Commands

Verify cowsay is installed and accessible.

> `command -v cowsay`

Check that it runs with default cow.

> `cowsay "Hello from snapshot testing!"`
```

**Правило формата простое:**
- Любая строка `` > `команда` `` — это команда для выполнения
- Всё остальное — описательный текст, проходит насквозь без изменений
- Runner подставляет вывод сразу после каждой команды в виде fenced code block

**Ключевое:** runner не нужен отдельный Dockerfile под каждый CLI. Ты указываешь `--base-image node:20-slim` — он генерирует Dockerfile на лету, собирает image, запускает `setup.sh` внутри, потом гоняет сценарии.

Ты можешь открыть сам `run-scenarios.sh` чтобы убедиться — он небольшой и читаемый. Но это не обязательно для понимания.

Что тебя больше удивляет в этой архитектуре — то что Dockerfile генерируется на лету, или то что формат такой минималистичный (просто `> \`команда\`` в Markdown)?

## UPD6

Мне нравится, что все сделано как документация, но при этом оно все еще часть запускной системы. Go

### RESULT

В точку! Это называется **Literate Testing** — идея что тест и документация это одно и то же артефакт, а не два разных файла которые нужно синхронизировать. В traditonal подходе у тебя README и тесты расходятся — README устаревает, тесты не читаются. Здесь: документ *и есть* тест.

Это ещё интереснее когда ты даёшь такой файл LLM на ревью: модель видит и "зачем" (описательный текст) и "что" (команда) и "факт" (вывод). Это намного больше контекста чем просто `assert output == "..."`.

Готов к Part 3 — там запустим реальный demo на твоей машине?

## UPD7 

Да, давай. Go

### RESULT

---

## 📖 Part 3: Запускаем Node.js demo

Docker у тебя уже установлен (обнаружен ещё в предыдущей сессии). Запускаем demo прямо сейчас.

Команда (запускаю из корня воркспейса):

```bash
bash modules/091-cli-testing/tools/cli-test-runner/run-scenarios.sh \
  --test-dir modules/091-cli-testing/tools/cli-test-runner/demo/node-cli \
  --base-image node:20-slim \
  --image-name demo-node-cli
```

Результат в терминале:
```
Building Docker image: demo-node-cli (base: node:20-slim) ...
[... Docker build (cached) ...]
Running scenarios in Docker...
  ✓ basic-commands.md
Done. 1 scenario(s) processed.
```

Все слои закешированы — второй запуск молниеносный.

После запуска файл `demo/node-cli/scenarios/basic-commands.md` заполнился выводом команд:

```markdown
> `cowsay "Hello from snapshot testing!"`
```
 ______________________________
< Hello from snapshot testing! >
 ------------------------------
        \   ^__^
         \  (oo)\_______
...
```

Вот это и есть **golden snapshot** — документ + вывод в одном файле, готовый к `git commit`.

Заметь строчку `✓ basic-commands.md` — runner пробежался по всем `*.md` файлам в папке `scenarios/` и выполнил команды в каждом. Добавишь новый файл в `scenarios/` — он автоматически попадёт в следующий запуск.

Как тебе формат вывода в Markdown — удобно читать `git diff` с такими cowsay-картинками, или кажется это слишком шумно для ревью?